# Deferred Work

## Deferred from: code review of 1-1-fundacao-e-login-com-papel-fixo (2026-08-10)

- Sem comportamento `ON DELETE` declarado nas FKs [`api/src/main/resources/db/migration/V1__schema.sql`] — nenhuma feature de exclusão existe ainda; default `NO ACTION` do Postgres é seguro por ora. Revisitar quando uma feature de exclusão (ex.: organizador cancelando sessão) for desenhada.
- `papel` é `String` solta no back (sem enum) e o front confia num cast não validado (`as T` em `web/src/api/client.ts:39`) sem `default` no switch de `rotaPorPapel` [`api/src/main/java/br/com/rolo35/api/auth/Usuario.java`; `web/src/pages/LoginPage.tsx:8`] — endurecimento de type-safety maior que o escopo desta story; nada quebra hoje porque front e back nascem da mesma fonte de verdade. Revisitar se um novo papel for adicionado ou se a resposta da API divergir do tipo esperado.

## Deferred from: code review of 1-2-busca-de-filmes-via-proxy-tmdb (2026-08-10)

- `spring.http.clients.connect-timeout`/`read-timeout` são propriedades globais do Spring Boot — qualquer `RestClient`/`RestTemplate` autoconfigurado que vier a existir no projeto herda silenciosamente o timeout 5s/8s pensado pro TMDb [`api/src/main/resources/application.properties`]. Revisitar se uma segunda integração de API externa precisar de timeout diferente.
- `TmdbMovieResult` não valida `id`/`title` nulos vindos do TMDb, enquanto `posterPath`/`releaseDate` têm guarda explícita [`api/src/main/java/br/com/rolo35/api/sessoes/catalogo/TmdbClient.java:63-68`]. Contrato do TMDb pra esses dois campos é estável na prática; revisitar se algum filme real disparar `tmdbId: null` no front.
- AC 2 (chave TMDb nunca no bundle) não tem verificação automatizada/CI — só checagem manual feita uma vez nesta sessão (`grep` no bundle de produção). Considerar um passo de build+grep no CI se o projeto ganhar pipeline de CI.
- Pacote `sessoes.catalogo` não segue literalmente o padrão `controller/service/repository` que as instruções do projeto listam como non-negotiable de camadas — decisão já registrada nos Dev Notes da Story 1.2, mas diverge do texto literal do non-negotiable. Revisitar se as instruções do projeto forem atualizadas pra abrir exceção explícita a pacotes finos feito este.
- `docs/decisions.md` (entrada "RestClient injetado...") descreve a tentativa anterior como "antes do commit" — impreciso após a reconstrução do histórico em commits separados (a versão com dois construtores foi de fato commitada em `45dd91b`, revertida só no `refactor` seguinte). Ajuste de texto cosmético.
- `key={filme.tmdbId}` em `BuscaFilmesPage` assume unicidade de `tmdbId` no array de resultados, nunca verificada contra a resposta real do TMDb. Risco baixo; revisitar só se o React acusar warning de key duplicada em uso real.

## Deferred from: code review of 2-1-criacao-de-sessao-com-bloqueio-de-conflito-de-horario (2026-08-10)

- **`idx_sessoes_sala_id` redundante** — a V3 criou `idx_sessoes_sala_id_data_hora`, do qual `idx_sessoes_sala_id` (V1) é prefixo. O índice simples agora só custa escrita e espaço. Precisa de uma migration própria de `DROP INDEX`; escopo fora desta story. Reavaliar junto da Story 2.3, que revisita índices de listagem pública.
- **`"LIVRE"` como string literal** — `SessaoService.java:87` duplica em código o `CHECK (status IN ('LIVRE','RESERVADO','VENDIDO'))` de `V1__schema.sql`, sem enum nem constante. Um typo só falha em runtime, como `DataIntegrityViolationException` → 500. Toda a Epic 3 (reservas) vai reusar esses valores — introduzir o enum lá, onde o conjunto completo de transições fica visível.
- **Duas gerações de Jackson convivendo** — `RestAccessDeniedHandler` importa `tools.jackson.databind.ObjectMapper` (Jackson 3, default do Spring Boot 4) enquanto `TmdbClient` importa `com.fasterxml.jackson.annotation.JsonProperty` (Jackson 2). Funciona hoje porque Jackson 3 ainda honra as anotações da 2, mas é armadilha silenciosa pra quem for adicionar serialização customizada. Pré-existente da Story 1.2.
- **Nenhuma guarda de rota no front-end** — `/organizador`, `/organizador/sessoes/nova`, `/cliente` e `/portaria` renderizam pra qualquer visitante (`web/src/App.tsx`). Não é falha de segurança (a autorização é checada no back a cada request), mas um CLIENTE logado preenche o formulário inteiro de criação de sessão antes de tomar 403, e um visitante sem token recebe a mensagem genérica "Erro ao comunicar com o servidor". Padrão pré-existente desde a Story 1.1 — resolver de uma vez com um componente de rota protegida.
- **Invariante de não-sobreposição não declarativa no banco** — a garantia de que duas sessões não coexistem sobrepostas na mesma sala vive só no par `findByIdForUpdate` + `existeConflitante` dentro de `SessaoService.criar`. Decisão do code review da Story 2.1: manter só o lock pessimista, que já é mecanismo de banco e cumpre o non-negotiable das instruções do projeto — a alternativa (`EXCLUDE USING gist` + `btree_gist` numa V4) acoplaria o buffer de 4h ao schema, onde hoje ele é uma constante única no service. **Ação obrigatória na Story 2.2** (edição de sessão): o `PUT` precisa replicar o mesmo par lock+checagem, senão produz sobreposição sem o banco reclamar.

## Deferred from: code review of 2-3-listagem-publica-de-sessoes (2026-08-10)

- **`listarPublicadas()` usa `JOIN assentos` (INNER)** — uma sessão cuja sala não tivesse nenhum assento cadastrado desapareceria da listagem pública, violando literalmente o AC1 ("todas aparecem"). Hoje inalcançável: `SessaoService.criar()` já rejeita com `SalaSemAssentosException` a criação de sessão pra sala sem assentos, então toda sessão existente sempre tem sala com assentos. Risco fica latente pra quando existir edição/remoção de assentos de uma sala já com sessões — nenhuma feature assim existe ainda. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java:31`]
- **`LEFT JOIN assento_sessao` sem tratamento pra assento órfão** — um assento sem linha correspondente em `assento_sessao` pra uma sessão específica não conta nem como livre nem como vendido na agregação, podendo subcontar `assentosLivres` e marcar `esgotada=true` indevidamente. Hoje inalcançável pela mesma razão do finding acima: `SessaoService.criar()` sempre popula `assento_sessao` pra 100% dos assentos da sala no momento da criação da sessão. Mesma causa raiz, mesmo gatilho de revisita (feature de edição de mapa de assentos pós-criação). [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java:28,32`]

## Deferred from: code review of 2-2-edicao-de-sessao-com-trava-pos-venda (2026-08-10)

- **Troca de sala em `editar()` apaga `assento_sessao` sem checar hold ativo** — a trava pós-venda (`existeIngressoConfirmado`) só consulta a tabela `ingressos`, nunca `reservas`/`assento_sessao.status='RESERVADO'`. Hoje inalcançável: nenhum código cria reserva/hold ainda (Epic 3 é backlog). **Ação obrigatória pra Epic 3**: quando reserva com hold temporário existir, `SessaoService.editar()` precisa checar hold ativo além de `existeIngressoConfirmado` antes de reescrever `assento_sessao` numa troca de sala — senão um cliente em checkout perde o assento sem aviso e a `reserva` fica órfã, apontando pra uma linha que não existe mais. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`findByOrganizadorId` usa `INNER JOIN assentos`** — uma sessão cuja sala não tenha assento cadastrado desapareceria da listagem de gestão do organizador, em vez de aparecer com `capacidade=0`. Mesma classe do achado já registrado acima pra `listarPublicadas` (Story 2.3): hoje inalcançável porque `SessaoService.criar()` rejeita sala sem assentos com `SalaSemAssentosException`. Mesmo gatilho de revisita. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **Ordem de lock assimétrica entre `criar()` (só sala) e `editar()` (sessão, depois sala)** — nenhum caminho do código trava sala antes de sessão em nenhum outro lugar, então não existe cenário concreto de deadlock hoje, mas a assimetria fica sem registro em nenhum Dev Note. Revisitar se algum fluxo futuro vier a travar sessão e sala em ordens diferentes na mesma transação. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`findByOrganizadorId` sem critério de desempate na ordenação** — `ORDER BY s.data_hora` sem `s.id` como tiebreaker; duas sessões no mesmo horário podem trocar de ordem entre requisições. Cosmético. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **Matriz de testes de segurança não exaustiva** — `SessaoSecurityTest` não cobre toda combinação papel×endpoint pros 3 endpoints novos (falta `PORTARIA` em `/minhas` e `PUT`, `CLIENTE` em `GET /{id}`). Valor marginal baixo: a mesma anotação `@PreAuthorize("hasRole('ORGANIZADOR')")` já está provada em pelo menos uma combinação por endpoint. [`api/src/test/java/br/com/rolo35/api/sessoes/SessaoSecurityTest.java`]
- **`EditarSessaoPage` não espelha a restrição `@Digits(integer=8, fraction=2)` do back no campo preço** — só descobre no round-trip da API em vez de bloquear no client como o resto do form. Cosmético, back já rejeita com segurança. [`web/src/pages/EditarSessaoPage.tsx`]
- **Sem índice em `sessoes.organizador_id` pra `findByOrganizadorId`** — a listagem de gestão (`GET /api/sessoes/minhas`) filtra por essa coluna sem índice de apoio. Decidido não aplicar agora (não atrapalha nenhuma regra de negócio definida, é ação de qualidade/performance); revisitar se a tabela `sessoes` crescer o suficiente pra tornar o full scan perceptível. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **`buscarPorId` calcula `capacidade` por uma fonte diferente de `listarMinhas`/`criar`** — `assentoSessaoRepository.findByIdSessaoId(id).size()` (linhas de `assento_sessao` da própria sessão) em vez do mapa de assentos da sala (`assentoRepository.findBySalaId`). As duas fontes concordam hoje por invariante (toda troca de sala reescreve `assento_sessao` 1:1 com o mapa da sala nova), mas nada no código garante isso permanentemente. Decidido não aplicar agora; revisitar se um caminho novo popular `assento_sessao` de forma parcial (ex.: cancelamento de assento individual). [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`editar()` trava a sala mesmo quando nem `salaId` nem `dataHora` mudam** — toda chamada de `editar()`, mesmo só trocando `preco`/`titulo`/`sinopse`, adquire `SELECT...FOR UPDATE` na sala e roda `existeConflitanteExcluindo`. Não quebra nenhuma regra de negócio (o resultado é sempre correto), só amplia a janela de contenção de lock. Decidido não aplicar agora; revisitar se `criar()`/`editar()` concorrentes na mesma sala começarem a esbarrar no `lock_timeout` de 3s em uso real. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`listarMinhas`/`buscarPorId` sem teste unitário direto em `SessaoServiceTest`** — hoje só exercitados indiretamente via `SessaoControllerTest` com o service mockado; a regra de ownership de `buscarPorId` nunca roda de fato em nenhum teste, embora a implementação esteja correta (mesmo padrão de `editar()`, que tem cobertura direta). `SessaoGestaoRepositoryTest.findByOrganizadorIdTrazSoAsProprias` também não prova o caso negativo (sessão de outro organizador ausente do resultado). Decidido não aplicar agora; revisitar antes de qualquer refactor nesses dois métodos. [`api/src/test/java/br/com/rolo35/api/sessoes/service/SessaoServiceTest.java`, `api/src/test/java/br/com/rolo35/api/sessoes/repository/SessaoGestaoRepositoryTest.java`]
- **`EditarSessaoPage` não checa `sessao.editavel` antes de renderizar o formulário** — quem chega numa sessão travada (link antigo, back-button, favorito) vê o form inteiro liberado e só descobre a trava depois de submeter e tomar `409` do back. A regra de negócio em si (trava pós-venda) é respeitada — o back nunca aceita a edição —, é só feedback reativo em vez de proativo na UI. Decidido não aplicar agora. [`web/src/pages/EditarSessaoPage.tsx`]

## Deferred from: code review of spec-corrige-seed-salas-sessao.md (2026-08-11)

- source_spec: `_bmad-output/implementation-artifacts/spec-corrige-seed-salas-sessao.md`
  summary: A geração de assentos em `V2__seed.sql` usa `chr(64 + fileira_num)` sem guarda pra `linhas > 26`, produzindo caracteres não-letra (`[`, `\`, `]`...) em vez de fileiras válidas.
  evidence: Achado do Blind Hunter. Não é alcançável hoje (maior sala seedada tem 10 linhas), mas o padrão foi generalizado nesta correção pra cobrir 3 salas e vai se repetir em qualquer sala nova; revisitar se uma sala com mais de 26 fileiras for seedada ou criada via gestão de salas.
- source_spec: `_bmad-output/implementation-artifacts/spec-corrige-seed-salas-sessao.md`
  summary: `SessaoConcorrenciaConflitoTest`, `SessaoConflitoHorarioTest` e `SessaoGestaoRepositoryTest` continuam usando `salaRepository.findAll().get(0).getId()` pra pegar uma sala de fixture, inconsistente com o padrão de busca determinística por nome já aplicado em `SalaAssentoRepositorySmokeTest` e `SessaoListagemRepositoryTest` nesta mesma correção.
  evidence: Achado do Edge Case Hunter. Inofensivo hoje porque nenhum desses 3 testes assert a identidade/capacidade da sala retornada, só usa o id pra criar uma sessão de fixture — mas fica como inconsistência de padrão dentro do mesmo diff. Ampliar o escopo desta correção pra tocar mais 3 arquivos de teste sem necessidade funcional não foi feito; revisitar numa limpeza futura de testes de `sessoes`.

## Deferred from: code review of 3-1-mapa-de-assentos-publico (2026-08-12)

- **Lista de assentos vazia sem feedback explícito na UI** — se `buscarMapaPorSessao()` (INNER JOIN) retornasse zero linhas, `MapaAssentosPage` renderiza uma grade vazia sem nenhuma mensagem, indistinguível de "ainda carregando". Hoje inalcançável: `SessaoService.criar()` sempre popula `assento_sessao` atomicamente junto da `Sessao` na mesma transação, então nenhuma sessão existente fica sem mapa de assentos. Revisitar se surgir algum caminho que crie `Sessao` sem popular `assento_sessao` (ex.: import/migração manual). [`web/src/pages/MapaAssentosPage.tsx`]
- **`new Date(mapa.dataHora)` sem tratamento defensivo pra data malformada** — renderizaria "Invalid Date" pro usuário. Hoje inalcançável: o back sempre serializa `LocalDateTime` válido via Jackson JSR310, não existe caminho de escrita que produza valor malformado. [`web/src/pages/MapaAssentosPage.tsx`]
- **Precisão decimal `BigDecimal` → `number` do JS via Jackson sem teste de ponta a ponta** — nenhum teste prova o arredondamento de um valor como `19.999` no pipeline completo (back → JSON → front → `toFixed(2)`). Padrão pré-existente, já usado do mesmo jeito em `SessaoListagemDto`/`SessaoGestaoDto`, não é regressão desta story. [`api/src/main/java/br/com/rolo35/api/sessoes/dto/MapaAssentosDto.java`]
- **Células de assento sem semântica de teclado/ARIA (`role`, `tabIndex`, handler)** — aceitável porque a Story 3.1 é explicitamente somente-leitura (Dev Notes: "Sem seleção/clique nesta story"). **Ação a considerar na Story 3.2**: ao adicionar seleção de assento, os elementos hoje `<div>` puros vão precisar de semântica de interação real, não é só reaproveitar o markup atual com um `onClick`. [`web/src/pages/MapaAssentosPage.tsx`]
- **`SessaoService.mapaAssentos()` sem filtro de janela de tempo** — sessões passadas continuam com mapa de assentos acessível via `GET /api/sessoes/{id}/mapa-assentos`, diferente de `listarPublicadas()` que filtra `data_hora >= now()`. Parece intencional (Dev Notes exigem que link direto/compartilhado da tela continue funcionando sem depender de `state` de navegação, incluindo refresh de página), não uma omissão. Revisitar só se um conceito de "sessão retirada/cancelada" for introduzido e precisar bloquear o mapa também. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]

## Deferred from: code review of 3-2-reserva-de-assentos-com-hold-temporario (2026-08-12)

- **`StatusAssento` criado mas não vira o tipo real de nenhum campo persistido** — `AssentoSessao.status` continua `String`, `SessaoService` continua com as constantes `STATUS_LIVRE`/`STATUS_RESERVADO`, e `ReservaService.statusEfetivoLivre()` só usa o enum via `.name().equals(String)` — nenhum type-safety real ganho ainda. Já registrado como opcional no próprio Dev Note da Story 3.2 ("não obrigatório pra fechar as ACs desta story"). Revisitar como um refactor dedicado se um code review futuro achar o `String` solto arriscado o bastante. [`api/src/main/java/br/com/rolo35/api/sessoes/StatusAssento.java`]
- **Sem teto pro total de holds ativos que um mesmo cliente acumula entre requisições separadas** — `MAX_ASSENTOS = 6` é checado por requisição, mas nada impede múltiplas chamadas a `POST /api/reservas` acumulando várias reservas `ATIVA` simultâneas do mesmo cliente. Fora do escopo das ACs da Story 3.2. Revisitar se abuso de hold virar um problema real de disponibilidade de assentos. [`api/src/main/java/br/com/rolo35/api/reservas/service/ReservaService.java`]
- **AC3 da Story 3.2 pede 403 pra requisição sem token, implementação devolve 401** — a implementação segue a convenção HTTP já estabelecida e consistente em toda a base (`SessaoSecurityTest` também usa 401 sem token / 403 com papel errado) — texto da AC é que está impreciso, não o código. Revisitar só se o time decidir padronizar 403 pra ausência de token em todo o projeto (mudança maior, fora do escopo de uma correção pontual). [`api/src/test/java/br/com/rolo35/api/reservas/ReservaSecurityTest.java`]
- **Tabela `reservas` sem índice além da PK** — nenhuma tela ainda consulta `reservas` por `sessao_id`/`cliente_id` (só `ReservaConcorrenciaConflitoTest`, e via `findAll().stream().filter()`, não produção). Revisitar quando uma consulta de produção nessas colunas existir (ex.: "minhas reservas" do cliente, Epic 4). [`api/src/main/resources/db/migration/V1__schema.sql`]

## Deferred from: code review of 4-1-confirmacao-de-pagamento-simulado-com-emissao-de-ingresso (2026-08-12)

- **Autorização de papel só no controller (`@PreAuthorize`), sem checagem própria no service** — `PagamentoService.confirmar()` confia inteiramente em `PagamentoController` ter o `@PreAuthorize("hasRole('CLIENTE')")`; o service não tem defesa em profundidade própria. Mesmo padrão de `ReservaService` em todo o projeto — não é regressão desta story, é a convenção estabelecida. Revisitar só se o service ganhar um segundo ponto de entrada (job em lote, ferramenta admin) sem passar pelo controller anotado. [`api/src/main/java/br/com/rolo35/api/pagamentos/service/PagamentoService.java`]
- **`Ingresso.createdAt` (`Instant`) e `Ingresso.validatedAt` (`LocalDateTime`) misturam tipos temporais** — mesma divisão já usada em `Reserva` (`createdAt: Instant`, `expiresAt: LocalDateTime`), não uma inconsistência nova. Revisitar como parte de um refactor dedicado de convenção temporal, se algum código futuro precisar comparar `validatedAt` com `createdAt` diretamente. [`api/src/main/java/br/com/rolo35/api/ingressos/Ingresso.java`]
- **`lock_timeout = '3s'` hardcoded na query nativa, não configurável via `application.properties`** — mesmo literal já usado em `ReservaService.reservar()`, replicado aqui por consistência, não introduzido como padrão novo. Revisitar se o timeout precisar variar por ambiente (ex.: mais folga em staging sob carga de teste). [`api/src/main/java/br/com/rolo35/api/pagamentos/service/PagamentoService.java`]

## Deferred from: code review of 4-2-meus-ingressos-e-link-publico (2026-08-12)

- **`buscarPublico()` faz 3 consultas sequenciais (`Ingresso`→`Sessao`→`Sala`) em vez de um `JOIN` único como `buscarPorCliente()`** — inconsistente dentro da mesma classe, mas é sempre exatamente 3 consultas fixas por chamada, não N+1 real que cresce com o resultado. Revisitar se o tráfego da rota pública de compartilhamento justificar a otimização. [`api/src/main/java/br/com/rolo35/api/ingressos/service/IngressoService.java`]
- **`permitAll()` de `/api/ingressos/*` em `SecurityConfig` é um catch-all** — qualquer rota `GET` futura de um segmento só sob `/api/ingressos/` herdaria `permitAll()` por padrão, a menos que um matcher mais específico e autenticado seja declarado antes dela (mesmo cuidado que `/minhas` já recebeu). Tradeoff deliberado, já documentado em `docs/decisions.md`. Revisitar se uma rota nova nesse formato for adicionada sem esse cuidado. [`api/src/main/java/br/com/rolo35/api/config/SecurityConfig.java`]
- **Side-channel de tempo entre "assinatura inválida" (resposta rápida, sem banco) e "assinatura válida mas não encontrado" (mais lenta, com banco) em `buscarPublico()`** — fora do modelo de ameaça declarado pela AC5, que é sobre conteúdo da resposta, não tempo de resposta. Revisitar só se um requisito explícito de resistência a timing attack for adicionado ao projeto. [`api/src/main/java/br/com/rolo35/api/ingressos/service/IngressoService.java`]
- ~~**`listarMinhas()` sem paginação**~~ — **resolvido.** O adiamento supunha que o volume não chegaria a importar, mas o critério era o tamanho do seed, não o desenho: a carteira gera um código assinado por linha devolvida, então o custo de abrir a tela cresce com o histórico do cliente e o seed cresce no CAP-13. Agora devolve o envelope `PaginaDto` de `GET /api/sessoes`, com teto de tamanho em `common.Paginacao`, `countQuery` próprio e desempate por `i.id` no `ORDER BY`. [`api/src/main/java/br/com/rolo35/api/ingressos/service/IngressoService.java`, `api/src/main/java/br/com/rolo35/api/ingressos/repository/IngressoRepository.java`]
- **`IngressoService.listarMinhas()`/`buscarPublico()` sem `@Transactional(readOnly = true)`** — mesmo padrão já usado pelos métodos de leitura de `SessaoService` (`listarMinhas`, `listarPublicadas`, `mapaAssentos`), nenhum deles anotado. Não é uma inconsistência nova desta story. [`api/src/main/java/br/com/rolo35/api/ingressos/service/IngressoService.java`]

## Deferred from: 4-3-checkout-de-pagamento-no-front (2026-08-12)

- **`MapaAssentosPage` trata `ASSENTO_INDISPONIVEL` e `ASSENTO_EM_DISPUTA` como o mesmo `409`** — os dois caem no mesmo ramo (`error.status === 409`), que limpa a seleção inteira e recarrega o mapa. `ASSENTO_EM_DISPUTA` é contenção momentânea de lock, não indisponibilidade confirmada: o cliente perde a seleção sem precisar. A ferramenta pra distinguir passou a existir nesta story (`ApiRequestError.codigo`), mas corrigir aquele call site está fora do escopo das ACs daqui. Revisitar junto de qualquer story que toque no fluxo de reserva. [`web/src/pages/MapaAssentosPage.tsx`]
- **`PagamentoService.confirmar()` monta `assentoIds` carregando a sessão inteira e filtrando em memória** — `findByIdSessaoId(sessaoId).stream().filter(reservaId)` traz todas as linhas de `assento_sessao` da sessão pra selecionar as poucas da reserva. A query `buscarAssentosDaReserva()` criada nesta story filtra por `reserva_id` no banco (agora com índice, V5) e resolveria o mesmo caso lendo só o necessário. Não aplicado aqui porque a story é explícita em não ampliar o back além do que as ACs pedem, e o caminho atual está correto — é custo, não defeito. Revisitar quando o `PagamentoService` for tocado por outro motivo. [`api/src/main/java/br/com/rolo35/api/pagamentos/service/PagamentoService.java`]
- **Nenhuma guarda de rota no front pra `/pagamento/:reservaId`** — um visitante sem token que abre a URL direto vê a tela de "esta reserva não é sua" em vez de ser levado ao login, diferente do que a AC8 faz no mapa de assentos. Não é falha de segurança (o back nega com `403` de qualquer forma) e o estado final é honesto, mas é o mesmo problema de guarda de rota já registrado no code review da Story 2.1 pra todas as áreas por papel — resolver de uma vez com um componente de rota protegida, não caso a caso. [`web/src/App.tsx`, `web/src/pages/PagamentoPage.tsx`]

## Deferred from: fechamento do Epic 5 (2026-08-14)

> O único achado crítico deste lote (Story 5.1 — `selecionarSessao()` sem checagem server-side de
> janela de tempo, non-negotiable de autorização) foi corrigido antes do fechamento, não deferido.
> Ver `5-1-selecao-de-sessao-do-turno.md`. O restante abaixo foi triado e descartado como não
> crítico pra permitir o fechamento do épico dentro do prazo.

- **`buscarSessaoAtiva()` mapeia qualquer `409` pra "sem sessão", ignorando `erro.codigo`** — contraria o racional documentado em `client.ts:11-14`. Mensagem genérica em vez de diferenciar motivo do 409. [`web/src/api/portaria.ts`]
- **`Promise.all` acopla endpoint público e autenticado em `SelecaoTurnoPortariaPage`** — `401`/`403` de `buscarSessaoAtiva()` é reportado como "não foi possível carregar as sessões", mensagem de outro contexto. [`web/src/pages/SelecaoTurnoPortariaPage.tsx`]
- **`turno_portaria.sessao_id` sem índice nem `ON DELETE`** — sem feature de exclusão de sessão hoje, `NO ACTION` é seguro; revisitar se isso mudar (mesmo critério já usado pra outras FKs do projeto). [`api/src/main/resources/db/migration/V6__turno_portaria.sql`]
- **Tabela `turno_portaria` no singular**, inconsistente com a convenção plural do resto do schema (`sessoes`, `salas`, `ingressos`) — cosmético, exigiria migration de rename. [`api/src/main/resources/db/migration/V6__turno_portaria.sql`]
- **`@Transactional(readOnly = true)` inerte em `obterSessaoAtivaOuLancar()`** (self-invocation não passa pelo proxy) e o método devolve a entidade `Sessao`, não um DTO — uso é interno, nunca serializado direto, sem vazamento de dado. [`api/src/main/java/br/com/rolo35/api/ingressos/service/PortariaService.java`]
- **`AssentoNaoEncontradoException` sem `@ExceptionHandler` → `500` genérico** — inalcançável na prática, FK garante assento válido pra todo ingresso. [`api/src/main/java/br/com/rolo35/api/ingressos/service/PortariaService.java`]
- **Estouro de lock cai no handler `handleSalaOcupada`**, mensagem de criação de sessão exibida a quem escaneia ingresso — comportamento (`503`, retry funciona) está correto, só a mensagem é de outro contexto. [`api/src/main/java/br/com/rolo35/api/ingressos/service/PortariaService.java`]
- **Teste de concorrência não afere que `validatedAt` não é sobrescrito na segunda chamada** — gap de cobertura, não bug de produção. [`api/src/test/java/br/com/rolo35/api/ingressos/PortariaValidacaoConcorrenciaTest.java`]
- **`dtoNaoExpoeCampoDeCliente` é tautologia** — reflete sobre nomes de campo do mesmo commit, passaria com um campo `comprador` disfarçado. [`api/src/test/java/br/com/rolo35/api/ingressos/service/PortariaServiceValidacaoTest.java`]
- **`assentoFileira && assentoNumero` esconderia assento se `numero === 0`** — inalcançável, `numero` de assento sempre começa em 1 (`generate_series(1, colunas)`). [`web/src/pages/ValidacaoPortariaPage.tsx`]
- **`#2E7D46` fora da paleta fixa, aplicado por `style` inline** — cosmético. [`web/src/pages/ValidacaoPortariaPage.tsx`]
- **Campo de digitação manual fora de `<form>`**: Enter não submete, leitor de código de barras keyboard-wedge não funciona; tela nunca mostra a sessão ativa — digitação manual continua funcional via botão, não é fluxo quebrado. [`web/src/pages/ValidacaoPortariaPage.tsx`]
- **Tipo TS `ResultadoValidacao` nomeia o envelope enquanto o enum Java homônimo é só o resultado** — obriga `ResultadoValidacao['resultado']`, cosmético. [`web/src/api/portaria.ts`]
- **Sem limite de tamanho em `codigo`** — rota exige papel `PORTARIA` autenticado, risco de abuso baixo. [`api/src/main/java/br/com/rolo35/api/ingressos/dto/ValidarIngressoRequest.java`]
- **`setUp()` privado chamado à mão em vez de `@BeforeEach`** — higiene de teste, sem impacto funcional. [`api/src/test/java/br/com/rolo35/api/ingressos/PortariaSecurityTest.java`]
- **Story 5.3 (Painel de Turno da Portaria) marcada `done` sem story file próprio e sem passar por `bmad-code-review` formal** — o `sprint-change-proposal-2026-08-13.md` (adendo) registra a decisão de implementação e as ACs, mas nunca foi materializada como `implementation-artifacts/5-3-*.md` nem triada como as demais stories. Fechada junto do épico por decisão explícita de prazo (penúltimo dia); revisitar se sobrar tempo — gerar o story file retroativo e rodar `bmad-code-review` nele antes da entrega final, ou aceitar a lacuna documentada aqui.

## Deferred from: code review da Story 1.3 (2026-08-14)

> Achados das três camadas de review que não são causados pela Story 1.3, ou cuja correção exigiria
> tocar `LoginPage.tsx` — que o usuário determinou que ficasse intocada nesta story. Os achados
> causados pela story (500 em e-mail duplicado concorrente, ausência de `@Size(max)`, lacunas de
> teste, documentação da allow-list) foram corrigidos, não deferidos.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: As ~18 linhas do letreiro de marquee estão duplicadas entre `LoginPage.tsx` e `CadastroPage.tsx`, e as duas telas divergem na largura máxima (`440px` contra `480px`) sem motivo declarado.
  evidence: A extração pra componente compartilhado chegou a ser feita durante a implementação e foi desfeita quando veio a instrução de não tocar `LoginPage.tsx` — um componente com um consumidor só não compartilha nada. Vira ganho real assim que a outra tela puder ser editada.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: `rotaPorPapel` mora em `LoginPage.tsx` e agora é importada página-a-página por `CadastroPage.tsx`; o lugar dela é ao lado de `salvarSessao`, em `web/src/lib/sessao.ts`.
  evidence: Acopla duas páginas sem relação entre si e alimenta os dois avisos de fast-refresh que o lint reporta (um módulo de componente exportando algo que não é componente). Mover exige editar `LoginPage.tsx`.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: `rotaPorPapel` é um `switch` exaustivo sem `default`, e o tipo TS `Papel` é um espelho mantido à mão do enum Java, sem teste de contrato entre os dois — um `papel` inesperado produz `navigate(undefined)`.
  evidence: A coluna `usuarios.papel` é `VARCHAR` com CHECK no banco, então o valor é constrangido na prática; mas o caminho passou a ter uma segunda porta de entrada com esta story. Corrigir exige editar `LoginPage.tsx`.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: Nenhum teste renderiza `App` — a tabela de rotas da aplicação não é observada por teste nenhum, em nenhuma rota.
  evidence: `grep -rn "import .*App" web/src` retorna só `src/main.tsx`. Reverter o `element` de `/cadastro` pro placeholder, ou escrever `path="/cadastrar"`, deixa a suíte inteira verde e desfaz a Task 9 sem sinal. Lacuna pré-existente do projeto, não introduzida aqui.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: `POST /api/auth/cadastro` é público e aceita o papel direto do corpo, sem rate limit, convite, captcha ou qualquer teto — qualquer pessoa na internet cria contas `ORGANIZADOR` ou `PORTARIA` ilimitadas.
  evidence: A ausência de gate de autorização é decisão deliberada e documentada (reduz atrito de avaliação do desafio), e o `AuthSecurityTest` fixa isso como esperado. O que não está decidido é o abuso por volume. Nenhuma AC pede controle de taxa; registrar antes de qualquer uso real.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: O contrato de requisição de `login()` em `web/src/api/auth.ts` — método, path e chaves do corpo — não é aferido por teste nenhum; as páginas mockam o módulo inteiro.
  evidence: Renomear o path ou uma chave do corpo deixa as duas suítes verdes e quebra o login em produção. É o padrão estabelecido do repositório (mockar o módulo de API nas páginas), pré-existente à Story 1.3. O mesmo buraco em `cadastrar()` foi fechado por esta story.

- source_spec: `_bmad-output/implementation-artifacts/1-3-cadastro-de-usuario.md`
  summary: `sprint-status.yaml` linha 44 não é YAML válido — o escalar de `last_updated` contém `Epic 5 fechado:` sem aspas.
  evidence: Presente desde `HEAD~10`, anterior a esta story. Não foi corrigido junto porque é um arquivo que a ferramentaria BMad reescreve, e mudar o estilo de aspas pode conflitar com as próprias escritas dela. Vale uma passada dedicada.
