# Deferred Work

## Deferred from: code review of 1-1-fundacao-e-login-com-papel-fixo (2026-08-10)

- Sem comportamento `ON DELETE` declarado nas FKs [`api/src/main/resources/db/migration/V1__schema.sql`] — nenhuma feature de exclusão existe ainda; default `NO ACTION` do Postgres é seguro por ora. Revisitar quando uma feature de exclusão (ex.: organizador cancelando sessão) for desenhada.
- `papel` é `String` solta no back (sem enum) e o front confia num cast não validado (`as T` em `web/src/api/client.ts:39`) sem `default` no switch de `rotaPorPapel` [`api/src/main/java/br/com/rolo35/api/auth/Usuario.java`; `web/src/pages/LoginPage.tsx:8`] — endurecimento de type-safety maior que o escopo desta story; nada quebra hoje porque front e back nascem da mesma fonte de verdade. Revisitar se um novo papel for adicionado ou se a resposta da API divergir do tipo esperado.

## Deferred from: code review of 1-2-busca-de-filmes-via-proxy-tmdb (2026-08-10)

- `spring.http.clients.connect-timeout`/`read-timeout` são propriedades globais do Spring Boot — qualquer `RestClient`/`RestTemplate` autoconfigurado que vier a existir no projeto herda silenciosamente o timeout 5s/8s pensado pro TMDb [`api/src/main/resources/application.properties`]. Revisitar se uma segunda integração de API externa precisar de timeout diferente.
- `TmdbMovieResult` não valida `id`/`title` nulos vindos do TMDb, enquanto `posterPath`/`releaseDate` têm guarda explícita [`api/src/main/java/br/com/rolo35/api/sessoes/catalogo/TmdbClient.java:63-68`]. Contrato do TMDb pra esses dois campos é estável na prática; revisitar se algum filme real disparar `tmdbId: null` no front.
- AC 2 (chave TMDb nunca no bundle) não tem verificação automatizada/CI — só checagem manual feita uma vez nesta sessão (`grep` no bundle de produção). Considerar um passo de build+grep no CI se o projeto ganhar pipeline de CI.
- Pacote `sessoes.catalogo` não segue literalmente o padrão `controller/service/repository` que o CLAUDE.md lista como non-negotiable de camadas — decisão já registrada nos Dev Notes da Story 1.2, mas diverge do texto literal do non-negotiable. Revisitar se o CLAUDE.md for atualizado pra abrir exceção explícita a pacotes finos feito este.
- `docs/decisions.md` (entrada "RestClient injetado...") descreve a tentativa anterior como "antes do commit" — impreciso após a reconstrução do histórico em commits separados (a versão com dois construtores foi de fato commitada em `45dd91b`, revertida só no `refactor` seguinte). Ajuste de texto cosmético.
- `key={filme.tmdbId}` em `BuscaFilmesPage` assume unicidade de `tmdbId` no array de resultados, nunca verificada contra a resposta real do TMDb. Risco baixo; revisitar só se o React acusar warning de key duplicada em uso real.

## Deferred from: code review of 2-1-criacao-de-sessao-com-bloqueio-de-conflito-de-horario (2026-08-10)

- **`idx_sessoes_sala_id` redundante** — a V3 criou `idx_sessoes_sala_id_data_hora`, do qual `idx_sessoes_sala_id` (V1) é prefixo. O índice simples agora só custa escrita e espaço. Precisa de uma migration própria de `DROP INDEX`; escopo fora desta story. Reavaliar junto da Story 2.3, que revisita índices de listagem pública.
- **`"LIVRE"` como string literal** — `SessaoService.java:87` duplica em código o `CHECK (status IN ('LIVRE','RESERVADO','VENDIDO'))` de `V1__schema.sql`, sem enum nem constante. Um typo só falha em runtime, como `DataIntegrityViolationException` → 500. Toda a Epic 3 (reservas) vai reusar esses valores — introduzir o enum lá, onde o conjunto completo de transições fica visível.
- **Duas gerações de Jackson convivendo** — `RestAccessDeniedHandler` importa `tools.jackson.databind.ObjectMapper` (Jackson 3, default do Spring Boot 4) enquanto `TmdbClient` importa `com.fasterxml.jackson.annotation.JsonProperty` (Jackson 2). Funciona hoje porque Jackson 3 ainda honra as anotações da 2, mas é armadilha silenciosa pra quem for adicionar serialização customizada. Pré-existente da Story 1.2.
- **Nenhuma guarda de rota no front-end** — `/organizador`, `/organizador/sessoes/nova`, `/cliente` e `/portaria` renderizam pra qualquer visitante (`web/src/App.tsx`). Não é falha de segurança (a autorização é checada no back a cada request), mas um CLIENTE logado preenche o formulário inteiro de criação de sessão antes de tomar 403, e um visitante sem token recebe a mensagem genérica "Erro ao comunicar com o servidor". Padrão pré-existente desde a Story 1.1 — resolver de uma vez com um componente de rota protegida.
- **Invariante de não-sobreposição não declarativa no banco** — a garantia de que duas sessões não coexistem sobrepostas na mesma sala vive só no par `findByIdForUpdate` + `existeConflitante` dentro de `SessaoService.criar`. Decisão do code review da Story 2.1: manter só o lock pessimista, que já é mecanismo de banco e cumpre o non-negotiable do CLAUDE.md — a alternativa (`EXCLUDE USING gist` + `btree_gist` numa V4) acoplaria o buffer de 4h ao schema, onde hoje ele é uma constante única no service. **Ação obrigatória na Story 2.2** (edição de sessão): o `PUT` precisa replicar o mesmo par lock+checagem, senão produz sobreposição sem o banco reclamar.

## Deferred from: code review of 2-3-listagem-publica-de-sessoes (2026-08-10)

- **`listarPublicadas()` usa `JOIN assentos` (INNER)** — uma sessão cuja sala não tivesse nenhum assento cadastrado desapareceria da listagem pública, violando literalmente o AC1 ("todas aparecem"). Hoje inalcançável: `SessaoService.criar()` já rejeita com `SalaSemAssentosException` a criação de sessão pra sala sem assentos, então toda sessão existente sempre tem sala com assentos. Risco fica latente pra quando existir edição/remoção de assentos de uma sala já com sessões — nenhuma feature assim existe ainda. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java:31`]
- **`LEFT JOIN assento_sessao` sem tratamento pra assento órfão** — um assento sem linha correspondente em `assento_sessao` pra uma sessão específica não conta nem como livre nem como vendido na agregação, podendo subcontar `assentosLivres` e marcar `esgotada=true` indevidamente. Hoje inalcançável pela mesma razão do finding acima: `SessaoService.criar()` sempre popula `assento_sessao` pra 100% dos assentos da sala no momento da criação da sessão. Mesma causa raiz, mesmo gatilho de revisita (feature de edição de mapa de assentos pós-criação). [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java:28,32`]

## Deferred from: code review of 2-2-edicao-de-sessao-com-trava-pos-venda (2026-08-10)

- **Troca de sala em `editar()` apaga `assento_sessao` sem checar hold ativo** — a trava pós-venda (`existeIngressoConfirmado`) só consulta a tabela `ingressos`, nunca `reservas`/`assento_sessao.status='RESERVADO'`. Hoje inalcançável: nenhum código cria reserva/hold ainda (Epic 3 é backlog). **Ação obrigatória pra Epic 3**: quando reserva com hold temporário existir, `SessaoService.editar()` precisa checar hold ativo além de `existeIngressoConfirmado` antes de reescrever `assento_sessao` numa troca de sala — senão um cliente em checkout perde o assento sem aviso e a `reserva` fica órfã, apontando pra uma linha que não existe mais. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`findByOrganizadorId` usa `INNER JOIN assentos`** — uma sessão cuja sala não tenha assento cadastrado desapareceria da listagem de gestão do organizador, em vez de aparecer com `capacidade=0`. Mesma classe do achado já registrado acima pra `listarPublicadas` (Story 2.3): hoje inalcançável porque `SessaoService.criar()` rejeita sala sem assentos com `SalaSemAssentosException`. Mesmo gatilho de revisita. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **Ordem de lock assimétrica entre `criar()` (só sala) e `editar()` (sessão, depois sala)** — nenhum caminho do código trava sala antes de sessão em nenhum outro lugar, então não existe cenário concreto de deadlock hoje, mas a assimetria fica sem registro em nenhum Dev Note. Revisitar se algum fluxo futuro vier a travar sessão e sala em ordens diferentes na mesma transação. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`findByOrganizadorId` sem critério de desempate na ordenação** — `ORDER BY s.data_hora` sem `s.id` como tiebreaker; duas sessões no mesmo horário podem trocar de ordem entre requisições. Cosmético. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **Matriz de testes de segurança não exaustiva** — `SessaoSecurityTest` não cobre toda combinação papel×endpoint pros 3 endpoints novos (falta `PORTARIA` em `/minhas` e `PUT`, `CLIENTE` em `GET /{id}`). Valor marginal baixo: a mesma anotação `@PreAuthorize("hasRole('ORGANIZADOR')")` já está provada em pelo menos uma combinação por endpoint. [`api/src/test/java/br/com/rolo35/api/sessoes/SessaoSecurityTest.java`]
- **`EditarSessaoPage` não espelha a restrição `@Digits(integer=8, fraction=2)` do back no campo preço** — só descobre no round-trip da API em vez de bloquear no client como o resto do form. Cosmético, back já rejeita com segurança. [`web/src/pages/EditarSessaoPage.tsx`]
- **Sem índice em `sessoes.organizador_id` pra `findByOrganizadorId`** — a listagem de gestão (`GET /api/sessoes/minhas`) filtra por essa coluna sem índice de apoio. Decidido não aplicar agora (não atrapalha nenhuma regra de negócio definida, é ação de qualidade/performance); revisitar se a tabela `sessoes` crescer o suficiente pra tornar o full scan perceptível. [`api/src/main/java/br/com/rolo35/api/sessoes/repository/SessaoRepository.java`]
- **`buscarPorId` calcula `capacidade` por uma fonte diferente de `listarMinhas`/`criar`** — `assentoSessaoRepository.findByIdSessaoId(id).size()` (linhas de `assento_sessao` da própria sessão) em vez do mapa de assentos da sala (`assentoRepository.findBySalaId`). As duas fontes concordam hoje por invariante (toda troca de sala reescreve `assento_sessao` 1:1 com o mapa da sala nova), mas nada no código garante isso permanentemente. Decidido não aplicar agora; revisitar se um caminho novo popular `assento_sessao` de forma parcial (ex.: cancelamento de assento individual). [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`editar()` trava a sala mesmo quando nem `salaId` nem `dataHora` mudam** — toda chamada de `editar()`, mesmo só trocando `preco`/`titulo`/`sinopse`, adquire `SELECT...FOR UPDATE` na sala e roda `existeConflitanteExcluindo`. Não quebra nenhuma regra de negócio (o resultado é sempre correto), só amplia a janela de contenção de lock. Decidido não aplicar agora; revisitar se `criar()`/`editar()` concorrentes na mesma sala começarem a esbarrar no `lock_timeout` de 3s em uso real. [`api/src/main/java/br/com/rolo35/api/sessoes/service/SessaoService.java`]
- **`listarMinhas`/`buscarPorId` sem teste unitário direto em `SessaoServiceTest`** — hoje só exercitados indiretamente via `SessaoControllerTest` com o service mockado; a regra de ownership de `buscarPorId` nunca roda de fato em nenhum teste, embora a implementação esteja correta (mesmo padrão de `editar()`, que tem cobertura direta). `SessaoGestaoRepositoryTest.findByOrganizadorIdTrazSoAsProprias` também não prova o caso negativo (sessão de outro organizador ausente do resultado). Decidido não aplicar agora; revisitar antes de qualquer refactor nesses dois métodos. [`api/src/test/java/br/com/rolo35/api/sessoes/service/SessaoServiceTest.java`, `api/src/test/java/br/com/rolo35/api/sessoes/repository/SessaoGestaoRepositoryTest.java`]
- **`EditarSessaoPage` não checa `sessao.editavel` antes de renderizar o formulário** — quem chega numa sessão travada (link antigo, back-button, favorito) vê o form inteiro liberado e só descobre a trava depois de submeter e tomar `409` do back. A regra de negócio em si (trava pós-venda) é respeitada — o back nunca aceita a edição —, é só feedback reativo em vez de proativo na UI. Decidido não aplicar agora. [`web/src/pages/EditarSessaoPage.tsx`]
